$(document).ready(function(){

  $('.tabs').tabs();

  /* Função tooltip */
  $('.tooltip').tooltip('methodName');
  var instance = M.Tooltip.getInstance(elem);

  /* Função select */
  $('select').formSelect();
  


});
